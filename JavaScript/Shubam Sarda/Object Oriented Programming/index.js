// Normal Array Creating 
const students = ["shubham", "alex", "deepak"];
console.log(students);

const ages = [12, 25, 20];
console.log(ages);

// Creating Array using new key word -> new is used to create a constructor of array instance.
const names = new Array("shubham", "alex");
console.log(names);

// Normal object Creating 
const info = {

}

console.log(info);

// Creating Object using new key word 
const customer = new Object();
console.log(customer);

// Everying in javaScript is object, well but its not direct truth but it is indirectly yes.
// string is also a object

// Creating an String
const name = "shubham";
console.log(name);

// Creating a String with new keyword
const product = new String("Water");
console.log(product);

console.log(name.toUpperCase());
console.log(product.toUpperCase());

// Everything in javaScript is object like Array, String, Number, Boolean, except null and undefined.

// -----------------------------------------------------------------------------------------------------------------------------------------------------------




