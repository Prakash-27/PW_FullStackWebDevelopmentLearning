// Syntax:

// for (initial expression; condition; update expression) {
//         for loop body
// }

for (let index = 0; index <= 10; index++) {
    if (index === 2) {
        // break
        continue
    }
    console.log(index);
}